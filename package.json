{
  "name": "worst-parking",
  "version": "1.0.0",
  "description": "Worst Parking App",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --names \"server,client\" \"npm run server --silent\" \"npm run client --silent\""
    "server": "npm start --prefix server nodemon index.js",
    "client": "npm start --prefix client react-scripts start",
    "dev": "concurrently --names \"server,client\" \"npm run server --silent\" \"npm run client --silent\""
  },
  "author": "DNS",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
